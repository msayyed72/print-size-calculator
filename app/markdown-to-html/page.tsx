"use client";
import Link from "next/link";import{useState}from"react";
export default function MarkdownToHtml(){const[text,setText]=useState("");const[out,setOut]=useState("");const convert=()=>{let x=text.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/`([^`]+)`/g,"<code>$1</code>").replace(/^### (.*)$/gm,"<h3>$1</h3>").replace(/^## (.*)$/gm,"<h2>$1</h2>").replace(/^# (.*)$/gm,"<h1>$1</h1>").replace(/\*\*(.+?)\*\*/g,"<strong>$1</strong>").replace(/\*(.+?)\*/g,"<em>$1</em>").replace(/\[(.+?)\]\((https?:\/\/[^\s)]+)\)/g,'<a href="$2">$1</a>').replace(/^(?:- )(.*)$/gm,"<li>$1</li>").replace(/\n{2,}/g,"</p><p>").replace(/^(?!<h|<li|<p)(.+)$/gm,"$1<br>");setOut("<p>"+x+"</p>")};return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">MARKDOWN</span></header><section className="toolPage"><div className="eyebrow">DEVELOPER TOOL</div><h1>Markdown → <em>HTML</em></h1><p className="toolIntro">Convert common Markdown syntax into HTML instantly.</p><div className="calculatorCard"><label className="toolLabel">Markdown<textarea className="toolTextarea" rows={10} value={text} onChange={e=>setText(e.target.value)} placeholder="# Hello\n\nWrite **bold** text here."/></label><button className="primaryButton fullAction" onClick={convert}>Convert to HTML</button>{out&&<label className="toolLabel">HTML<textarea className="toolTextarea" rows={10} readOnly value={out}/></label>}</div><p>Conversion runs entirely in your browser.</p><p><Link href="/">← Back to FameOrbit</Link></p></section></main>;}