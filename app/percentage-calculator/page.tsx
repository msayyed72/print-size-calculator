"use client";
import Link from "next/link";
import {useMemo,useState} from "react";
export default function PercentageCalculator(){const [mode,setMode]=useState("percent");const [a,setA]=useState("50");const [b,setB]=useState("200");const n=(v:string)=>Number(v)||0;const result=useMemo(()=>mode==="percent"?n(a)*n(b)/100:mode==="change"?(n(b)-n(a))/n(a)*100:mode==="what"?n(a)/n(b)*100:n(a)-n(b),[a,b,mode]);return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">CALCULATOR</span></header><section className="hero"><div className="eyebrow">PERCENTAGE CALCULATOR</div><h1>Calculate percentages <em>instantly.</em></h1><p className="lead">Find percentages, percentage changes, and differences with a simple browser-based calculator.</p><div className="panel"><div className="panelHead"><h2>Calculator</h2><p>Choose what you want to calculate.</p></div><div className="custom"><select className="toolInput" value={mode} onChange={e=>setMode(e.target.value)}><option value="percent">What is X% of Y?</option><option value="change">Percentage change from X to Y</option><option value="what">X is what % of Y?</option><option value="difference">Percentage-point difference (X − Y)</option></select><div className="inputs"><label>{mode==="percent"?"PERCENT": "FIRST VALUE"}<input type="number" value={a} onChange={e=>setA(e.target.value)}/></label><span className="unit">{mode==="percent"?"%":""}</span><label>SECOND VALUE<input type="number" value={b} onChange={e=>setB(e.target.value)}/></label></div><div className="customResult"><strong>{Number.isFinite(result)?result.toFixed(2):"—"}{mode==="percent"||mode==="change"||mode==="what"?"%":""}</strong><p>{mode==="percent"?`${a}% of ${b} is ${result.toFixed(2)}.`:mode==="change"?`The percentage change is ${result.toFixed(2)}%.`:mode==="what"?`${a} is ${result.toFixed(2)}% of ${b}.`:`The difference is ${result.toFixed(2)}.`}</p></div></div></div></section><section className="contentSection"><div className="contentInner"><h2>How percentage calculations work</h2><p>To find a percentage of a number, multiply the number by the percentage and divide by 100. For percentage change, divide the difference by the original value and multiply by 100.</p></div></section><footer><Link href="/">← All FameOrbit tools</Link></footer></main>}
