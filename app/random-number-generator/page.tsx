"use client";
import Link from "next/link";
import {useState} from "react";
export default function RandomNumberGenerator(){const [min,setMin]=useState("1"),[max,setMax]=useState("100"),[count,setCount]=useState("1"),[result,setResult]=useState<number[]>([]);function generate(){const lo=Math.ceil(Number(min)),hi=Math.floor(Number(max)),n=Math.min(100,Math.max(1,Number(count)||1));if(!Number.isFinite(lo)||!Number.isFinite(hi)||lo>hi){setResult([]);return}const out=Array.from({length:n},()=>Math.floor(Math.random()*(hi-lo+1))+lo);setResult(out)}return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">RANDOM</span></header><section className="hero"><div className="eyebrow">RANDOM NUMBER GENERATOR</div><h1>Pick a number <em>at random.</em></h1><p className="lead">Generate one or many random integers between your chosen minimum and maximum.</p><div className="panel"><div className="custom"><div className="inputs"><label>MINIMUM<input type="number" value={min} onChange={e=>setMin(e.target.value)}/></label><label>MAXIMUM<input type="number" value={max} onChange={e=>setMax(e.target.value)}/></label><label>HOW MANY<input type="number" min="1" max="100" value={count} onChange={e=>setCount(e.target.value)}/></label></div><button className="primaryAction fullAction" onClick={generate}>Generate numbers ↗</button>{result.length>0&&<div className="customResult"><strong>{result.join(", ")}</strong><p>Generated between {min} and {max}.</p></div>}</div></div></section><footer><Link href="/">← All FameOrbit tools</Link></footer></main>}
