"use client";
import Link from "next/link";
import {useState} from "react";
export default function SlugGenerator(){const[text,setText]=useState("");const[slug,setSlug]=useState("");const make=()=>setSlug(text.normalize("NFKD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,""));return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">SLUG GENERATOR</span></header><section className="toolPage"><div className="eyebrow">TEXT TOOL</div><h1>URL Slug <em>Generator</em></h1><p className="toolIntro">Turn titles and phrases into clean, SEO-friendly URL slugs.</p><div className="calculatorCard"><label className="toolLabel">Title or phrase<input className="toolInput" value={text} onChange={e=>setText(e.target.value)} placeholder="My Amazing Blog Post!"/></label><button className="primaryButton fullAction" onClick={make}>Generate slug</button>{slug&&<div className="resultBox"><div className="resultRow"><span>Slug</span><strong>{slug}</strong></div></div>}</div><p>Letters are normalized, spaces and punctuation become hyphens, and extra hyphens are removed.</p><p><Link href="/">← Back to FameOrbit</Link></p></section></main>;}