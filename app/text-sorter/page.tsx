"use client";
import Link from "next/link";
import {useState} from "react";
export default function TextSorter(){const[text,setText]=useState("");const[result,setResult]=useState("");const sort=(mode:string)=>{const lines=text.split(/\r?\n/).filter(x=>x.trim()!=="");lines.sort((a,b)=>mode==="length"?a.length-b.length:mode==="desc"?b.localeCompare(a,undefined,{numeric:true,sensitivity:"base"}):a.localeCompare(b,undefined,{numeric:true,sensitivity:"base"}));setResult(lines.join("\n"));};return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">TEXT SORTER</span></header><section className="toolPage"><div className="eyebrow">TEXT TOOL</div><h1>Text <em>Sorter</em></h1><p className="toolIntro">Sort lines alphabetically, reverse alphabetically, or by length.</p><div className="calculatorCard"><label className="toolLabel">Input<textarea className="toolTextarea" rows={10} value={text} onChange={e=>setText(e.target.value)} placeholder="One item per line..."/></label><div className="buttonRow"><button className="primaryButton" onClick={()=>sort("asc")}>A → Z</button><button className="secondaryButton" onClick={()=>sort("desc")}>Z → A</button><button className="secondaryButton" onClick={()=>sort("length")}>By length</button></div>{result&&<label className="toolLabel">Sorted result<textarea className="toolTextarea" rows={10} readOnly value={result}/></label>}</div><p><Link href="/">← Back to FameOrbit</Link></p></section></main>;}