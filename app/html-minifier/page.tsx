"use client";
import Link from "next/link";import{useState}from"react";
export default function HtmlMinifier(){const[text,setText]=useState("");const[out,setOut]=useState("");const run=()=>setOut(text.replace(/<!--[\s\S]*?-->/g,"").replace(/>\s+</g,"><").replace(/[\t\r\n]+/g," ").trim());return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">HTML MINIFIER</span></header><section className="toolPage"><div className="eyebrow">DEVELOPER TOOL</div><h1>HTML <em>Minifier</em></h1><p className="toolIntro">Reduce HTML whitespace and remove standard comments for a more compact document.</p><div className="calculatorCard"><label className="toolLabel">HTML<textarea className="toolTextarea" rows={12} value={text} onChange={e=>setText(e.target.value)} placeholder="Paste HTML here..."/></label><button className="primaryButton fullAction" onClick={run}>Minify HTML</button>{out&&<label className="toolLabel">Minified HTML<textarea className="toolTextarea" rows={10} readOnly value={out}/></label>}</div><p>This performs conservative whitespace/comment minification in your browser.</p><p><Link href="/">← Back to FameOrbit</Link></p></section></main>;}