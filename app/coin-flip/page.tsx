"use client";
import Link from "next/link";
import {useState} from "react";
export default function CoinFlip(){const [flips,setFlips]=useState(1),[results,setResults]=useState<string[]>([]);function flip(){setResults(Array.from({length:Math.min(100,Math.max(1,flips))},()=>Math.random()<.5?"Heads":"Tails"))}return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">RANDOM</span></header><section className="hero"><div className="eyebrow">COIN FLIP</div><h1>Flip a coin <em>instantly.</em></h1><p className="lead">Need a quick random choice? Flip a virtual coin as many times as you like.</p><div className="panel"><div className="custom"><label className="toolLabel">NUMBER OF FLIPS<input className="toolInput" type="number" min="1" max="100" value={flips} onChange={e=>setFlips(Number(e.target.value)||1)}/></label><button className="primaryAction fullAction" onClick={flip}>Flip coin ↗</button>{results.length>0&&<div className="customResult"><strong>{results.join(" · ")}</strong><p>Heads: {results.filter(x=>x==="Heads").length} · Tails: {results.filter(x=>x==="Tails").length}</p></div>}</div></div></section><footer><Link href="/">← All FameOrbit tools</Link></footer></main>}
