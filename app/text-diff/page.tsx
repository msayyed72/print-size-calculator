"use client";
import Link from "next/link";import{useState}from"react";
export default function TextDiff(){const[a,setA]=useState("");const[b,setB]=useState("");const[out,setOut]=useState<string[]>([]);const compare=()=>{const x=a.split(/\r?\n/),y=b.split(/\r?\n/),n=Math.max(x.length,y.length);setOut(Array.from({length:n},(_,i)=>{const left=x[i]??"",right=y[i]??"";return left===right?`  ${left}`:`- ${left}\n+ ${right}`}))};return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">TEXT DIFF</span></header><section className="toolPage"><div className="eyebrow">TEXT TOOL</div><h1>Text <em>Diff</em></h1><p className="toolIntro">Compare two blocks of text line by line and spot additions and removals.</p><div className="calculatorCard"><div className="formGrid"><label className="toolLabel">Original<textarea className="toolTextarea" rows={10} value={a} onChange={e=>setA(e.target.value)} placeholder="Original text..."/></label><label className="toolLabel">New version<textarea className="toolTextarea" rows={10} value={b} onChange={e=>setB(e.target.value)} placeholder="New text..."/></label></div><button className="primaryButton fullAction" onClick={compare}>Compare text</button>{out.length>0&&<pre className="toolTextarea" style={{whiteSpace:"pre-wrap",overflowX:"auto"}}>{out.join("\n")}</pre>}</div><p>Comparison happens entirely in your browser.</p><p><Link href="/">← Back to FameOrbit</Link></p></section></main>;}