"use client";
import Link from "next/link";import{useState}from"react";
export default function JsonMinifier(){const[text,setText]=useState("");const[out,setOut]=useState("");const[error,setError]=useState("");const run=()=>{try{setOut(JSON.stringify(JSON.parse(text)));setError("")}catch{setOut("");setError("Invalid JSON. Please check the syntax.")}};return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">JSON MINIFIER</span></header><section className="toolPage"><div className="eyebrow">DEVELOPER TOOL</div><h1>JSON <em>Minifier</em></h1><p className="toolIntro">Remove unnecessary JSON whitespace and reduce the output to its compact form.</p><div className="calculatorCard"><label className="toolLabel">JSON<textarea className="toolTextarea" rows={10} value={text} onChange={e=>setText(e.target.value)} placeholder="Paste formatted JSON here..."/></label><button className="primaryButton fullAction" onClick={run}>Minify JSON</button>{error&&<p className="errorText">{error}</p>}{out&&<label className="toolLabel">Minified JSON<textarea className="toolTextarea" rows={8} readOnly value={out}/></label>}</div><p>Validation and minification happen locally in your browser.</p><p><Link href="/">← Back to FameOrbit</Link></p></section></main>;}