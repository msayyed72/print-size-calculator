"use client";
import Link from "next/link";
import {useState} from "react";
export default function ColorConverter(){const[hex,setHex]=useState("#6366f1");const[rgb,setRgb]=useState("rgb(99, 102, 241)");const convert=(v:string)=>{let h=v.trim().replace(/^#/,'');if(h.length===3)h=h.split('').map(x=>x+x).join('');if(!/^[0-9a-fA-F]{6}$/.test(h)){setRgb("Invalid HEX color");return}const n=parseInt(h,16);setHex('#'+h.toUpperCase());setRgb(`rgb(${n>>16}, ${(n>>8)&255}, ${n&255})`)};return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">COLOR CONVERTER</span></header><section className="toolPage"><div className="eyebrow">DESIGN TOOL</div><h1>Color <em>Converter</em></h1><p className="toolIntro">Convert HEX colors to RGB quickly and copy the result.</p><div className="calculatorCard"><label className="toolLabel">HEX color<input className="toolInput" value={hex} onChange={e=>setHex(e.target.value)} onBlur={()=>convert(hex)} placeholder="#6366f1"/></label><div className="resultBox"><div className="resultRow"><span>HEX</span><strong>{hex}</strong></div><div className="resultRow"><span>RGB</span><strong>{rgb}</strong></div></div><button className="primaryButton fullAction" onClick={()=>convert(hex)}>Convert</button></div><p>Supports 3-digit and 6-digit HEX colors. Conversion happens entirely in your browser.</p><p><Link href="/">← Back to FameOrbit</Link></p></section></main>;}