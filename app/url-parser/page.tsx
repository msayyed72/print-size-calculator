"use client";
import Link from "next/link";
import {useState} from "react";
export default function UrlParser(){const[url,setUrl]=useState("");const[data,setData]=useState<URL|null>(null);const[error,setError]=useState("");const parse=()=>{try{setData(new URL(url));setError("")}catch{setData(null);setError("Please enter a valid URL.")}};return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">URL PARSER</span></header><section className="toolPage"><div className="eyebrow">DEVELOPER TOOL</div><h1>URL <em>Parser</em></h1><p className="toolIntro">Break any URL into its protocol, hostname, port, path, query and hash.</p><div className="calculatorCard"><label className="toolLabel">URL<input className="toolInput" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://example.com/path?name=value#section"/></label><button className="primaryButton fullAction" onClick={parse}>Parse URL</button>{error&&<p className="errorText">{error}</p>}{data&&<div className="resultBox">{[["Protocol",data.protocol],["Hostname",data.hostname],["Port",data.port||"—"],["Path",data.pathname],["Query",data.search||"—"],["Hash",data.hash||"—"]].map(([k,v])=><div className="resultRow" key={k}><span>{k}</span><strong>{v}</strong></div>)}</div>}</div><p>Everything is parsed locally in your browser.</p><p><Link href="/">← Back to FameOrbit</Link></p></section></main>;}