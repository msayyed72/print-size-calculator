"use client";
import Link from "next/link";
import {useState} from "react";
export default function UrlEncoderDecoder(){const[text,setText]=useState("");const[result,setResult]=useState("");const[error,setError]=useState("");const run=(mode:string)=>{setError("");try{setResult(mode==="encode"?encodeURIComponent(text):decodeURIComponent(text));}catch{setResult("");setError("This text contains an invalid encoded URL sequence.");}};return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">URL ENCODER</span></header><section className="toolPage"><div className="eyebrow">DEVELOPER TOOL</div><h1>URL Encoder <em>Decoder</em></h1><p className="toolIntro">Safely encode or decode URL text directly in your browser.</p><div className="calculatorCard"><label className="toolLabel">URL or text<textarea className="toolTextarea" rows={8} value={text} onChange={e=>setText(e.target.value)} placeholder="https://example.com/search?q=hello world"/></label><div className="buttonRow"><button className="primaryButton" onClick={()=>run("encode")}>Encode</button><button className="secondaryButton" onClick={()=>run("decode")}>Decode</button></div>{error&&<p className="errorText">{error}</p>}{result!==""&&<label className="toolLabel">Result<textarea className="toolTextarea" rows={8} readOnly value={result}/></label>}</div><p>Your input is processed locally in the browser.</p><p><Link href="/">← Back to FameOrbit</Link></p></section></main>;}