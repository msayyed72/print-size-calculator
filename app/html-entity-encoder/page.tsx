"use client";
import Link from "next/link";
import {useState} from "react";
export default function HtmlEntityEncoder(){const[text,setText]=useState("");const[result,setResult]=useState("");const encode=()=>setResult(text.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&#39;"));const decode=()=>{const el=document.createElement("textarea");el.innerHTML=text;setResult(el.value)};return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">HTML ENTITIES</span></header><section className="toolPage"><div className="eyebrow">DEVELOPER TOOL</div><h1>HTML Entity <em>Encoder</em></h1><p className="toolIntro">Encode special HTML characters into entities, or decode them back to normal text.</p><div className="calculatorCard"><label className="toolLabel">Input<textarea className="toolTextarea" rows={8} value={text} onChange={e=>setText(e.target.value)} placeholder={'<div>Hello & welcome</div>'}/></label><div className="buttonRow"><button className="primaryButton" onClick={encode}>Encode</button><button className="secondaryButton" onClick={decode}>Decode</button></div>{result!==""&&<label className="toolLabel">Result<textarea className="toolTextarea" rows={8} readOnly value={result}/></label>}</div><p>Conversion happens locally in your browser.</p><p><Link href="/">← Back to FameOrbit</Link></p></section></main>;}