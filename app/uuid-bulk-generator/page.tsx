"use client";
import Link from "next/link";import{useState}from"react";
export default function UuidBulk(){const[count,setCount]=useState(10);const[out,setOut]=useState<string[]>([]);const generate=()=>{const n=Math.min(100,Math.max(1,Math.floor(count)));setOut(Array.from({length:n},()=>crypto.randomUUID()))};return <main><header className="siteHeader"><Link className="brand" href="/"><strong>Fame</strong>Orbit</Link><span className="headerTag">UUID BULK</span></header><section className="toolPage"><div className="eyebrow">DEVELOPER TOOL</div><h1>Bulk UUID <em>Generator</em></h1><p className="toolIntro">Generate up to 100 unique UUID v4 identifiers instantly.</p><div className="calculatorCard"><label className="toolLabel">Number of UUIDs<input className="toolInput" type="number" min="1" max="100" value={count} onChange={e=>setCount(Number(e.target.value))}/></label><button className="primaryButton fullAction" onClick={generate}>Generate UUIDs</button>{out.length>0&&<label className="toolLabel">Generated UUIDs<textarea className="toolTextarea" rows={12} readOnly value={out.join("\n")}/></label>}</div><p>UUIDs are generated locally using the browser's cryptographic random generator.</p><p><Link href="/">← Back to FameOrbit</Link></p></section></main>;}